import { StyleSheet } from "react-native";

export const MeassgeStyle = StyleSheet.create({
  header: {
    flexDirection: "row",
    justifyContent: "flex-end",
    marginRight: 5,
    height: 50,
    borderRadius: 10,
  },
});
