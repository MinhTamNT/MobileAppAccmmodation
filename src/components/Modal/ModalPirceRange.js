import { View, Text, Modal } from "react-native";
import React from "react";

const ModalPirceRange = () => {
  return (
    <Modal animationType="slide" transparent={true}>
      <View
        style={{
          flex: 1,
          justifyContent: "flex-end",
        }}
      >
        <View></View>
      </View>
    </Modal>
  );
};

export default ModalPirceRange;
